entity Player {
    var speed = 6
    var jumpForce = 12

    on start {
        print "Double C* Player loaded!"
    }

    on update {
        if key W { move(0, -speed) }
        if key S { move(0, speed) }
        if key A { move(-speed, 0) }
        if key D { move(speed, 0) }
    }
}
